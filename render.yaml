databases:
  - name: marriagedb
    databaseName: mysite
    user: mysite
    plan: free

services:
  - type: web
    name: lms-spike
    runtime: ruby
    buildCommand: ./bin/render-build.sh
    startCommand: bundle exec rails db:migrate && ./bin/thrust ./bin/rails server -p 10000
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: marriagedb
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false
      - key: RAILS_SERVE_STATIC_FILES
        value: "true"
